{
  "mcpServers": {
    "logosdb": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "logosdb-mcp-server"],
      "env": {
        "LOGOSDB_PATH": "${HOME}/.claude/.logosdb"
      }
    }
  }
}
